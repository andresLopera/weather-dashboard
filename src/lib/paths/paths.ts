/**
 * Enum representing the paths in the application.
 * @enum {string}
 */

enum Paths {
  // Pages
  Root = '/',
  Dashboard = '/home/dashboard',
  OtherPage = '/home/otherPage'
}

export default Paths
